node_modules
public/images/drafts/
.next/
.DS_Store

# Local environment files (keep .env.example tracked)
.env
.env*.local
.env.production

# Claude Code local tooling (settings, launch config, worktrees)
.claude/
